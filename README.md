# ImplementationBinaryLaegna
Mapping Laegna numbers to binary systems.

Laegna is made to be optimal system for automation, and so is binary:
- I did not leave anything out from experience what I have gained from binary automata.

Laegna R=0,5 (Z); 1 (X); 2 (Y) => zero (Z), 1 (X) and infinity (Y). These are three main octaves or projections of numbers: other octaves condense their absolute extremes as limit values taken to infinite repetition, of other octaves as well. Because infinity can only be rescaled, not cut - rescaling, look from outwards, is cutting.

Zero => Infinity maps to diagonal as T => R; Zero => One maps as lower T => R pair (IA <=> OE or a <=> o; one by multiplication-division) and One => Infinity as higher T => R pair (IO <=> AE; zero by addition negation).

Normal binary system:
- Able for 0-bit virtual containers (counted 0-bit slice), altough virtually.
- Able for 1-bit, 2-bit, 4-bit, 8-bit, 16-bit, 32-bit, 64-bit fields and their small-bit-perfect or longer-bit-perfect sizes, such as 4 2-bit field makes sense in assembler.
  - Remapped syntax can take *linear amount of slowdown*, because operations are nearly-compatible, with counted special cases and different I/O format: laegna number along with decimal number, along with variable and function names and other syntax so function Aa() is different from variable Aa or number Aa, so sometimes you want to discern numbers such as 0Aa, #Aa, Aa# - where "#" can classically refer to well-ordered numbers, which behaves like integer, such as a product code #AEEA-1221-4696, which is maybe manually assigned to this specific product rather than A34E-ERRT-ERDA went to random product which cannot be remembered, because it survives well the memory of the product - for example, when you last saw that lighter and was it *the same lighter* or *the same series*, where series might have manually ordered, and art can be specially chosen, or government/their corporation can have short, codes.

In Laegna I call Soviet Union also a "Corporation", for it's main headquarters - and "Communism", "Left-Hand", "Socialist" are all left - it's not an ideology, but a mathematical structure which can be discussed, also in terms of it's self-establishment.
- This way I make sure what "Corporations" are - for example, what is part of University, which calls itself a Corporation?
  - In Latin I only find Corporate Body, and Christ as well is organizing Corporate Body - A Corporation of Totality, as it's clear from algorithm; Buddha is creating Republic mentally, but He said one should not care initially whether corporation - God - exists or not, because with this small experience even corporation expects you to live your own life and only pay for products you purchase: if they see heavy experience in their field, they might feel you might get truly aware of your corporation, and God or Community of Your would rather hire you, where initially they want you to happily enjoy their products, as biological product or fruit of themselves would.
- So mathematically, I just measure this: either top-down, or bottom-up, and then the rational coupling and integration of concept - can such entities live together, can they live at all? The latter can be an opinion, but it's not immediately clear from the base math: if you can leave them always alive, and always integrate, other solutions become irrelevant; if you can not - you are kind of paradox mind or binary relevant, linear thinker of either the small, or the large.

Base-2 O and A:
- Fit into bit, byte 1/8, etc.
- Perfect fit storage.
- Perfect fit operation, but: for as long as binary operation holds, it is converted to few bits longer binary number.

Base-4 I, O, A, E:
- Fit into 2 bits, byte 1/4 etc.
- Perfect fit storage.
- Perfect fit operation, but: for as long as binary operation holds, it is converted to few bits longer binary number.

Base-16 / base-4 digits in pairs:
- Fits into hexadecimal notation.

Laegna base-18 (1 to 8):
- Fits into octal.

Hexadecimals and octals have known symmetries.

Each can be represented by:
- Z, 0 digits - different dimensionalities of 0 in each of 3 systems. Value can set only from class, metaphysically - zero-D at position 6.3 remains zero-D at position 6.3, unless there is some relation with external system, for example 0 can relate to next-band 0 - to infinity symmetry by basic opposition, a logical arithmetic: zero exists as logic, as graph, as each-point-as-closed system, as parallel to infinity in lower domain.

- X / Z, 1 digits - their inner curvature unit-projected to infinity. Simplest real model.

- _ / X, 2 digits - 2 is projective to infinity.

- Y, value-count digits: digit positions are absolutely synchronous to values.

In each case, same matrix can be applied to values and positions, so the diagonal exists and value is trivially infinity-relational, laegna-symmetric scale.

The other scales:
- Interpolating relations such as 7 / 3 give basic repetitions like 21 as lowest denominator, then to base-2 it forms infinite series; locally the larger combinations are best related in base-4 or base-2 or base-16 laegna Z, X, or Y-dimensional digit system in floating point manner, and with two channels there is outwards-correction fix, and octave relation which repeats in infinity, or their projection to exponent-accelerated linearized outwards-symmetric scale in same optimizer range and clocktick-matrix, which essentially seems linear altough it's not: number system is linear, imaginary reality behind it is rather curved and fractal holographic, but it's not relevant if you implement basic operations: this is relevant if you actually measure octaves to infinity relations in physical and life systems as well as their spirit and arhetypal-symbolic realities under surreal rules of infinities. You can understand infinity is something you use in everyday basis: poetic, symmetric, aligned to mind and matter as it's optimization goal-curve and it's positive metaresonance, archetypal and symbolic projecting class to instance, in holistic manner. It's also a business or carmic curve, a long-term coherence of their short-term impulses and goals, which are refined and integrated, internal-external holistic systems. Holistic means "whole-integrated" and here, various words can be proposed.

Mantissa:
- Basically behavioral to octaves:
  - Each one-unit-growth if it's precisely symmetric to binary, is also some symmetric or exp-symmetric scale to octaves.
  - Octaves can be defined in relation to mantissas.
  - So you project Z-X-Y to mantissa.

Backend can:
- Convert Laegna to binary in quite straightforward way, and use some bits or bytes or times larger containers when numbers are in local storage.
- Cache them and keep the ownership flag.

Frontend can:
- Convert them to Laegna numbers.
- Represent them as Laegna numbers.
- Keep ownerships and caches.
- Do front-end and high-order math operations.

Both systems can orchestrate a correspondence:
- Binary system is Laegna system.

I basically think that also "combinatoric level" should be applied to variable so that it can be symbolically post-processed, if you highlight certain parts of your operations as special:
- While boundary and choice cases or best working approximations should be already correct in real time, based on final symmetries kept, such as boundary values.
- This post-symbolic processing makes relevant, selected values, rather than large-range determinant processing and fine maps, into precise, exact values of symbolic kind, such as "1" can be 0, 1, 1, 2 series - first or second 1, which responds to step(thisvalue) => nextvalue: a = start_iterator(series) = 0, b = step(a) = 1, c = step(b) = 1, d = step(c) = 2, where 1, now, is necessarily *two distinct values in terms of this series, and what can be proven to be their associations*. In Laegna, A and AA are two octaves of 1, which can be mapped easily as their *values differ*, altough *their T values don't* - R is either 1 or 2, T is 1.
  - A and AA difference can be kept in certain mantissa-field relations, such as with certain *unit* which corresponds to 1 in calculations; calculations must correspond.
    - Now, in local storage, laegna number is like 3 *times* larger memory-wise, than in it's global, inactive form where the results, their aggregators, diagonals, dimensional reductions (all three are different words about same mathematical positioning, but not in terms of units and real-life or logical-physical-spirit correspondences). Spirit, in Laegna goal-state Automation, correspond about *what the boolean Ten knows about itself, and what is it's goal* - for example, it's internal value might correspond to external game, but it might be different, such as lie or beteryal; Laegna system can map all these "life" terms, but this information - it has goal-state, such it can refer to "spirit", but it's not a real cognitive entity; sincerity of human - whether the bit-correspondence is real to it's external correspondence, if the value is modelled that external bit calculation is not resonant, or can not model the internal bits to that degree - but rather, if they have real consequences or some expressions or expression-wide and operation-wide implication which leads to function-value correspondences, which have unknowns (U), but otherwise refer to tensor fields: Z (physical) => "dead" matter, local intercation, tensor field builds combinatorics and estimations or oracles around energy efficiency and equilibrum; Y (spiritual) => spirit of human and community, unit-wise interaction, objects and subjects and objectivity and reality interact as parts and wholes; X (metal-space-aetheric coordination in terms of matter) => robot and AI use estimated tensor fields for their coordination and release of tensions between their input and output, in coherence flow of the number and it's matrix-based correspondence, the word and it's token - a numeric coordinate.

OpenGL estimates:
- Make square fields in sizes such as 0\*0 (imaginary, does not need representation) - constant, only needs a position of it's system but does not have position inside - it's member of graph and logical correspondence, and upside-down U is used in Laegna to exponent zero, which does not have position outside, where other exponent numbers has - it's member of graph which looks like ethical, spiritual, life-community-unity-related, repeated games or infinite fractal, poetry symbolics, archetype or class etc.; in number realm, those magnitudes are reflected in local, multidimensional angle in every correspondence.
  - Digit is single, multi, or if you want zero or higher-band-zero dimensional representation of it's whole, and infinitesimal-infinity reflection is always represented in curving realm, altough limit values appear at very different directions, scales, dimensions and approximations, so it's rather a living coordinate even in metaphysical space - you still move the camera, and it's responded by something time-like, which positions you into mathematically poetic realm of higher identities of numbers, their selves vs. egos - the static state of where they are.

All these sized, such as 64\*64, 256\*256 (unsigned laegna field), 512\*512 (signed laegna field, 2\*2 256-based matrixes) makes real sense to represent Laegna numbers and their very presice operations and relations become handled by OpenGL system, which perfectly works on these 2D fields - matrixes, which rule object positioning are 2D, and their point lists are 1D vectors as well as their connections barely connect two tesimals of second D in connecting line with two ends. The rest is - intermediate calculations of this automation class and instance, variable system into it's *projection*, mathematically precise representation of our 3D realm in many visible, obvious, measurable local terms.

# X, Y, Z

Let's take two octaves: R and T.
- Branch each to three octaves: Z, X, Y.

X is "unit": it corresponds to heart and solar plexus coordination in it's IO (solar plexus, ego - subject and object realm, unit realm centered around 1) and AE (heart, self, inter-subject and inter-object creative high-d grid yet talking about units in infinite repetition, so one cannot exist without other - in 1:2 vs. 2:1 correspondence in 4 channels creating a relation in Laegna) coordination.

Z is "particle": it corresponds to quantas, to infinitesimals, to inner realms and their projections to infinity, because Z and Y are very symmetric - X is linear, single dimension, while those provide 2 additional dimensions of same degree, of center, of negative and positive scales. So they are scale-mappings of 2 (octave-recursive map) or infinity (limit-value-recursive map). Take limit values of limit values - a second degree. Can you estimate this? $Y^2 = YY$ in Laegna octave numbers when they project infinity, not 2, into Y - both systems have same boundary names, minus-plus infinity, zero, two infinities.

Boundaries:
- W - minus infinity, zero boundary. Extreme smallest.
- Upside-down V, V if V is not present - plus infinity, two-infinity boundary. Extreme largest in any case.
- U, plus zero - plus zero, or median average limit from upwards, from upside-down U which can be represented as V in 6-based system / unit scale X plus.
- V, minus zero - minus zero, or median average limit from downwards, from W or zero or minus infinity in signed attitude.
- https://spireason.neocities.org/Playground/SheepCounter1/sheepiotae.html - ordinary number counter of Laegna indeed, has only the simplest boundary case where each R value has exactly 4 boundaries, and each boundary value can be used only as single, repeated digit, not mixed with I, O, A, E ordering - the natural order of Laegna which is calm and never messed up, even if it's 00.000, but rather if it's AAAAaaaa or AAaa or Aa, three corresponding octaves Y => X => Z in model scale of otherwise same amount, but Aa is a small step which repeated many times over it's single-instance boundary value, becomes class and then meta-/superclass, before it precisely, at point, reaches AAAAaaaa: in reality, rather they appear as measurement points, because you rather bother to repeat the same digit infinitely in real life if it's in higher scale, but not in the limit, so in each case it does *not* project to extreme gaussian limits in their ideal forms, but rather your infinite A looks like: AAOOAAOEOOA, where you have sometimes noticed it's not averaging to A, and doing corrective or balancing corrections to your directions, rather than exactly A, exactly constant 1, every day - so the measurement system is slowdown by half, to 0.5 => 0.25 is a bad estimation scale (around 1 / 3 by eye); Daisy and Elise cats represent that: Elise is doing 1.111111111, but with bad elise it becomes 1.1112111011011220001111111 and suddenly 3, which is very unexpected move for everybody. Daisy is beyond all the chaos, but still a very serious mathematical model which is slowly building something on it, unexpactable yet clever; elise is pure love and alone, would not survive a random number in the ethical field - really present, such as a poor man in otherwise thriving community, how to even explain this mathematically? Daisy would not believe math model where this is not inside probabilistic resolution of goals, rather than optimizing math of something very growing, stable, relevant to all.

Boundaries, thus, can be kept with pointers:
- Number table with numbers.
- Pointer table with their positions.
  - Type information in dynamic system.
  - Memory allocation table in static system.
  - Single instance of boundaries, for each number in 2 bits, is kept along or if very instance-specific, then 2-bit field corresponds to type-bit field:
    - 8-bit tables of R=4 B=4, 4-digit base-4 numbers contains all orders of I, O, A, E, always in four.
    - This type of indexor tables:
      - Repeat each relation.
      - Boundary values are kept in simple tables along, together with any special values: they are exact, smaller-digit-number copy of this order of 8-bit tables.
        - 8-bit numbers can multidimensionally use other scales to correspond boundaries, or contain just corresponding value to being in boundary, in which case bit flags must just not correspond to number value if it's literal and not boundary.

Octaves:
- Z: values, zero to single-digit. 1D, because each is applied local value-order matrix and values locally convert to matrix with all cells empty / 0 / False, one cell 1 / True.
- X: positions, single to one to value-digits systems are fine for diagonal or real projections. 2D, because positions can be applied to values which are already contained in zero-to-one dimension, otherwise it would be rather one-or-self-digits.
- Y: boundaries. As you scale and scale, but this type of boundaries keeps only the most distant limit values straight in your hands.
  - If you go to more complexity, yet you need more complexities in boundaries, but this is multi-external - whatever your digit system, always these four boundaries applie as 4, so the four is relevant to bases in their 0, 1, 2, 3 bits corresponding to I, O, A, E dimensionalities (0, 1, 2, 3) - this is one value which meets others when octaves are applied, such as actually having such dimensionality matrix (mathematical, curve emulation - Logecs logic) or field (datasystem, physical emulation - Logecs mathematic, as much as one can even divide Logecs into two groups: I have some vague design of math-logic polarity inside logecs itself, even if it largely unifies it on actual calculation level and difference is somewhat semantical, such as A AND B and A * B numerically looking the same, altough one can track what they measure).

So:
- Value: numbers, bitwise quite harmonious.
  - Convert to normal binary but more bits, do your operations and convert back, apply symbolic enchance on subset of steps such as precise averages or totals, made by symbolic processor, while mass-field-data processor such as showing a movie, works primitively.

2-4 times slower processing could happen: this is fine.

You find more bit-wise correspondences to Laegna, but I was contemplating:
- Let's say it's 2-4 times slower.
- Let's say your semantic optimization has much more powers.
- It's more powerful than assembler, scaling more than classic syntax.

# Actual dimensional correspondences.

Let's provide 6 octaves:
- R octaves Z, X, Y.
- T octaves Z, X, Y.

Mean system initially is correspondence to physics and it's math modelling, which is most precise numerically - same relations can be applied to spiritual or digital system, but we can see time, space, coordinates, relations, infinities - all in physical measure; we work our math and creativity on the same unit measure.

# X

T - our body as symbolic ball, exact sphere, so that it's unit size is not very different in infinity, if something like 1m is used as 1 unit of human scale of unitary body, and single human can use their single body.

R - vision field is local exponent. What you see, head: and the local-infinity effect is *outwards collapse*.

Visible light, hearable sound, feltable vibration, livable environment - those define the unit scales, even to "place", because all places are not places in human sense - you cannot visit a galaxy, keeping your hands tigtly around it, but a livable place inside.

# Z

This is quantum, more subtle than zero, and also the base units for material constitution - if's unit size is single atom, *smallest of our linear space*, and it scales down to quantum and less very fast, estimating light-vibration, all kinds of wave curvatures if projected to similar features of the number system.

TZ - our biological body matter, almost alive but external in terms our senses and visions rather have symbolic meaning in sense of material, cells give meaning to it rather than measuring exact, precise physice. TZ is yet, somewhere around biological body - already almost external, material, measurable - and it's base matter - not very concievable, external process to spirit inside.

RZ - our present environment matter, next scale our ecosystem matter which would be RRZ, R-squared Z because two digits in Laegna, is also one digit squared in it's ideal properties, for limit digits but also for others - they can be squared because of precision, even middle- or down-wards-to-their-zero.

# Y

When our feltable collapses, infinitesimal of Y - the external is born.

This infinitesimal reaches unit somewhere around our linear boundaries:
- Where ball projects, in each visible term, to straight flat surface, circle to flat edge, line to it's limit of infinite repetition, never-touchable and almost imaginary point yet visible in line, infinity of infinitesimals of coordinates - a single coordinate, next whole number, such as 0 to 0.(9) is really, each precision once counted, but 1 is not - line ends or starts with limit value, if origin is the other boundary. So even boundaries can be funny and Laegna system is designed to avoid this surprise level in it's structure, rather than any special operations: moving in any direction in it's own terms, it keeps some consistency - for example, imprecision it much more bound to scoped imprecision in scopes, rather than coordinate-butterfly-domino effects and chains leading to chaotic horizons and athmospheres, and non-fractal vision of Zeroes present in Nature, or non-linear way to see linear inertial systems in linexp linearity of Lane Geometry of Laegna - in Laegna Lane repos and LaeAutomation, various Lane databases already exists, somewhat funny limit to make it base-4 compatible is applied for R=0.5, where I did not want special non-RT space but added it somehow to base-4 1 digit space, so actually octave up precision, but R = 1; 2; 3; 4 are perfect. I thought: who understands the thing, can build base-2 perfectly themselves, while beginner should not create space where there is not R and T bit for every digit, because it would be exception - normally, base-2 digit is one digit of pair of base-4 digit projection, either diagonal or parallel - Den is not single entity of Laegna, but Tens can be projected to two Dens, multidigit Tens to multidigit Dens, and it's always trackable what digits are meant, even approxiable if they are nearly linear, exponent, log, or any combination herein - close systems can be found in terms of real values, and their imprecision is known by digit length, infinite series representation or lack of, and symbolic co and post-processing where *Laegna digit naturally understands it's growing precision and effort can be tautologically described, to accept intermediate, low-combinatoric result, but try on to more combinatorics - system interactions and self-environmental reflections are all tautologically trivial, first-order cityzens so laegna first order is not exactly "non-self-referencial"*.

First-order self-referentiality:
- V is self-referential by symmetry.
- External repetition is symmetric fractal, linear scale is focused, and inwards the fractal is similar to moles and graphs of molecular structures - the fractal-hologram heads.
- Even machines, in complex systems, become linear entities but this is complex - simple self-referrial gets accelerated point value, and the external operation is perfectly symmetric to linar, which would even be optimized to future result in Logex-Ten-like-interaction of linear exponent to linear-local degree; the fact you turn the shape of this number, by this external operation, is already doing the linear resymmetrization. The case is that: it's repetitive fractal, in every symmetry to now, is inside the number scale and result you get is symmetric, and we are using very symmetric and basic mathematical and numeric relations to guarantee this without intervention. It's even able to track illusions, and operate in unknowns, in same kind of fractal-symmetric approximation space, altough Laegna U and V can initially not seem very powerful - they seem like accepting only *certain unknown zones*, but it's not lack of power: it's that 4-based system is trivial, and 6-based extension is as trivial as statistics can get, but assuming there are 46 or 33 different, interesting numbers about probability effects is rather an image gallery, or a special theory, than mathematical notation - ten digit values, four operations in classic latin system are enough to build up a whole handheld calculator and any logic, even exception cases are added in small number like 6 cases about what could ever be wrong - such as trying to write digit to screen which is full, or trying to divide by zero, are a few known exceptions.
  - In this sense: 6-value statistics is the absolute core basis, and in fact it becomes imaginative and powerful very fast, with a few digits, where it's special values *still start to carry special meanings*. Than doing anything else than precise 0, 1, 2, 4 symmetries - I could not do anything else, nor having more than 2 values for clearly, and axe and not a spectacle of biological life forms or myriad of creatures. Just numbers, rather, can be made of two values, two digits, a few scales, nothing twelve-fold and heavily geometric, altough twelve-fold is one of smallest multidimensional geometries which really makes, if you learn all combinations of twelve values, and their nuances and differences under projections, all the randomization and probability appear smooth machine: I think this is very interesting if you are statistician, and you are ready to watch one-hour movies of variations of field relations, maybe you just get enlightened and say you know the relation, is, inside there and you can see it.
    - This means: I became very happy as more simple relations I found in two digits, and I saw them *naturally becomes more and more trivial*, and the thing is like logic - not like anything unhuman.
      - For example, in automation: both ends have one band, both bands have local probability 1:1 or none, but any other probability value is complex, self-referential math of their interaction.
      - For example, linear or exponent degree can have probability which is either +1/-1 dice, or +2/-2 dice, but additional letter for linexp - is like edge case, altough not complex to express. I see how easy is to define anything on this simple degree, and it looks like *first-order*, like something one can extend, move, apply transforms, meanings and exact measures, units and identities to. The 12-fold continuous geometry is something an advanced mathematician of their few, special math and computational and graphical domains, might suddenly make seem like natural beauty - it looks like Mandlebrot, a surprising aspect or viewpoint to reveal it all, and how it appears in nature, but the name of man is rather even remembered and associated with the image; this is a long story, a story of heavy success rather than basic primitive for math school: use use 2 or 12-positional U-V systems to represent every relation and disposition, but in normal U and V, multidimensional projections already define nice chess-fields and some understandings of *what having boundaries -inf, -0, +0 and +inf, or 0, unit-identity-median-, unit-identity-median+, \["2"\]infinities - what this actually means.

